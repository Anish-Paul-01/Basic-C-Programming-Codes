#include<stdio.h>
#include<math.h>
 
 // Use The Library Function to Calculate thr Area of a square with side a.

int main(int argc, char const *argv[])
{
    int side;
    printf("Enter the Value of Side \n");
    scanf("%d", &side);
    printf("The Value of Area is %f", pow(side,2));


    return 0;
}
    