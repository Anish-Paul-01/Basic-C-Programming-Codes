#include<stdio.h>
#include<string.h>

int main(int argc, char const *argv[])
{
    char *st ="Anish" ;
    int a = strlen(st); //Count the number of charater in string excluding the null character

    printf("The Length of string st is %d", a);
    return 0;
}
